import React from "react";
import styles from 'styles/Item.module.css';

const Item = () => {
    // event handler for dibs button

    return (
        <div>
            {/* user name (linked to their profile), item image, caption, dibs button */}
            Im an Item!
        </div>
    )
}

export default Item;